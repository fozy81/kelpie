
{{#let this.uniqueForms as |uniqueforms|}}
  {{#each uniqueforms as |form|}}  
    <FormCard
      @model={{form}}
      @allforms={{@forms}}
      @formTemplates={{@formTemplates}}
    />
  {{/each}}
{{/let}}

{{yield}}