

<div class="flex wrap">   
<LinkTo @route="projects" class="py-2 flex-1" > 
<div class="m-2 p-2 w-4/5 text-blue-600 border border-gray-200 rounded-lg"> 
 ᐸ Back to Projects
     </div>
</LinkTo> 
 
<div class="relative">
   <Menu @label="project" @title="...Show menu" class="flex-1" /></div>
   </div>
<div style="background-color:{{{@model.colour}}};" class="p-2 rounded-lg border-gray-400 border-2">
<p></p>
<div   class="font-bold">{{@model.title}} </div>

<p></p>
{{@model.description}}
<p></p>

<DueDate @due={{@model.dueDate}} @id={{@model.id}}/>


<hr>
Tasks:


{{#each @model.tasks as |task|}}

<Card @title={{task.title}}
       @route="task"
       @model={{task.id}} 
       @colour="#edf2f7"/>
       {{else}}
       No tasks assigned
{{/each}}

 </div>
<CreateCard @label="Add New Task"
            @modelName="task"/>

           

{{outlet}}